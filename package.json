{
	"name": "glance",
	"version": "0.1.0",
	"description": "One WebGL2 surface that draws many line and image plots into rectangles.",
	"license": "MIT",
	"author": "Philipp Thölke",
	"repository": "github:dav0dea/glance",
	"type": "module",
	"files": [
		"dist"
	],
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"prepare": "npm run build",
		"check": "tsc --noEmit",
		"test": "vitest run"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vitest": "^4.1.7"
	}
}
